"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.paginateListSecurityProfilesForTarget = void 0;
const ListSecurityProfilesForTargetCommand_1 = require("../commands/ListSecurityProfilesForTargetCommand");
const IoTClient_1 = require("../IoTClient");
const makePagedClientRequest = async (client, input, ...args) => {
    return await client.send(new ListSecurityProfilesForTargetCommand_1.ListSecurityProfilesForTargetCommand(input), ...args);
};
async function* paginateListSecurityProfilesForTarget(config, input, ...additionalArguments) {
    let token = config.startingToken || undefined;
    let hasNext = true;
    let page;
    while (hasNext) {
        input.nextToken = token;
        input["maxResults"] = config.pageSize;
        if (config.client instanceof IoTClient_1.IoTClient) {
            page = await makePagedClientRequest(config.client, input, ...additionalArguments);
        }
        else {
            throw new Error("Invalid client, expected IoT | IoTClient");
        }
        yield page;
        const prevToken = token;
        token = page.nextToken;
        hasNext = !!(token && (!config.stopOnSameToken || token !== prevToken));
    }
    return undefined;
}
exports.paginateListSecurityProfilesForTarget = paginateListSecurityProfilesForTarget;
