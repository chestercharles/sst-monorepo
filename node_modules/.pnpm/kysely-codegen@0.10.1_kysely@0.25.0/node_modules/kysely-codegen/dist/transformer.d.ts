import { Definitions, Imports, Scalars } from './adapter';
import { EnumCollection, SymbolCollection } from './collections';
import { Dialect } from './dialect';
import { DatabaseMetadata } from './metadata';
import { ExportStatementNode, ExpressionNode, ImportStatementNode } from './nodes';
export type TransformContext = {
    camelCase: boolean;
    defaultScalar: ExpressionNode;
    defaultSchema: string | null;
    definitions: Definitions;
    enums: EnumCollection;
    imports: Imports;
    metadata: DatabaseMetadata;
    scalars: Scalars;
    symbols: SymbolCollection;
};
export type TransformOptions = {
    camelCase: boolean;
    defaultSchema?: string;
    dialect: Dialect;
    metadata: DatabaseMetadata;
};
/**
 * Transforms database metadata into a TypeScript-compatible AST.
 */
export declare class Transformer {
    #private;
    transform(options: TransformOptions): (ExportStatementNode | ImportStatementNode)[];
}
