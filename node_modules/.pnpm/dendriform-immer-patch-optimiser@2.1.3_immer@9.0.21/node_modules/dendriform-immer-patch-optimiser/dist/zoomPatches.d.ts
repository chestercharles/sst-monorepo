import type { DendriformPatch, Path } from './types';
import type { Patch as ImmerPatch } from 'immer';
export declare const zoomInPatches: <P extends ImmerPatch | DendriformPatch>(path: Path, patches: P[]) => P[];
export declare const zoomOutPatches: <P extends ImmerPatch | DendriformPatch>(path: Path, patches: P[]) => P[];
