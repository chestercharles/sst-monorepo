import { n as nodeHTTPRequestHandler } from '../../../dist/nodeHTTPRequestHandler-2e6cc2d7.esm.js';
import 'url';
import '../../../dist/transformTRPCResponse-f73dc4a2.esm.js';
import '../../../dist/codes-fe07bf82.esm.js';

/* eslint-disable @typescript-eslint/no-explicit-any */
function createExpressMiddleware(opts) {
  return (req, res) => {
    const endpoint = req.path.substr(1);
    nodeHTTPRequestHandler({ ...opts,
      req,
      res,
      path: endpoint
    });
  };
}

export { createExpressMiddleware };
